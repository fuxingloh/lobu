name: smoke
description: Basic smoke test — agent responds and doesn't error
trials: 1
timeout: 60
tags: [smoke, ci]

turns:
  - content: "Hello"
    assert:
      - type: regex
        value: ".{10,}"
