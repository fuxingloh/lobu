// Peerbot Configuration
// This file is generated by @peerbot/cli
// Learn more: https://github.com/buremba/peerbot

module.exports = {
  // Worker configuration
  worker: {
    // How to customize the worker:
    // - 'base': Use the default peerbot-worker-base image (fastest)
    // - 'dockerfile': Customize via Dockerfile.worker (recommended)
    // - 'image': Use a pre-built custom image
    customization: '{{WORKER_CUSTOMIZATION}}',

    // Base image for worker (versioned with CLI)
    baseImage: 'buremba/peerbot-worker-base:{{CLI_VERSION}}',

    // Resource limits (applied per worker)
    resources: {
      cpu: '1000m',    // 1 CPU core
      memory: '2Gi',   // 2GB RAM
    },

    // Additional environment variables for workers
    environment: {
      // Add custom env vars here
      // EXAMPLE_VAR: 'value',
    },

    // Volume mounts (for Docker/K8s)
    volumes: [
      // { host: './data', container: '/workspace/data', readOnly: true },
    ],

    // Storage configuration
    storage: {
      workspace: {
        type: 'persistent',  // or 'ephemeral'
        size: '10Gi',
      },
    },

    // Scaling configuration
    scaling: {
      max: 10,              // Maximum concurrent workers
      idleTimeout: '60m',   // Cleanup idle workers after this duration
    },
  },

  // Gateway configuration
  gateway: {
    port: 8080,
    publicUrl: process.env.PEERBOT_PUBLIC_GATEWAY_URL,
  },

  // Credentials (loaded from .env)
  credentials: {
    slack: {
      botToken: process.env.SLACK_BOT_TOKEN || '',
      appToken: process.env.SLACK_APP_TOKEN || '',
    },
    anthropic: {
      apiKey: process.env.ANTHROPIC_API_KEY || '',
    },
  },

  // Platform-specific overrides (optional)
  targets: {
    docker: {
      compose: {
        projectName: '{{PROJECT_NAME}}',
      },
    },

    // Uncomment and configure for Kubernetes deployment:
    // kubernetes: {
    //   namespace: 'default',
    //   registry: 'docker.io',
    //   repository: 'myorg/peerbot-worker',
    //   releaseName: 'peerbot',
    //   runtimeClassName: 'gvisor',
    //   ingress: {
    //     enabled: true,
    //     className: 'nginx',
    //     host: 'peerbot.mycompany.com',
    //   },
    // },

    // Uncomment and configure for Cloudflare Containers:
    // cloudflare: {
    //   accountId: process.env.CLOUDFLARE_ACCOUNT_ID,
    //   maxInstances: 10,
    //   storage: {
    //     workspace: {
    //       bucket: 'peerbot-workspace',
    //     },
    //   },
    //   routing: 'stateful',
    // },
  },
};
