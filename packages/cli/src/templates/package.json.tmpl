{
  "name": "{{PROJECT_NAME}}",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "peerbot dev",
    "logs": "peerbot logs",
    "down": "peerbot down",
    "rebuild": "peerbot rebuild",
    "deploy": "peerbot deploy"
  },
  "dependencies": {
    "@peerbot/cli": "^{{CLI_VERSION}}"
  }
}
